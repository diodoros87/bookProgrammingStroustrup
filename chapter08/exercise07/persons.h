using std::string;
using std::vector;

void validate_name (const string& s);
void validate_age (double age);
double get_age(const string& s);

extern vector<string> age_ordered_names;
extern vector<double> global_vec_age;
